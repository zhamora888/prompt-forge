---
topic: PromptForge (working title) — mobile prompt library PRD
updated: 2026-07-08
---

- (note) user delivered a fully-scoped brain dump: mobile app to organize/search/reuse AI prompts across tools (Claude, ChatGPT, Gemini, Cursor, Windsurf); core story "save a prompt, find and reuse it later"
- (note) MVP feature list given verbatim: Prompt Management (CRUD), Search (title/content/tags), Favorites (toggle + filter), Copy Prompt (one-tap clipboard), Prompt Categories (fixed dropdown, no CRUD)
- (note) Prompt data shape given verbatim: id, title, content, category, tags[], isFavorite, createdAt, updatedAt
- (decision) tech stack: React Native, Tailwind (NativeWind) for styling — routed to addendum.md as implementation detail, not PRD content
- (decision by user) stakes: this is a take-home/technical assessment evaluated by a company; simple app but must demonstrate solid PM thinking
- (decision by user) working mode: Fast path — batch remaining gaps into one round of questions, draft full PRD with [ASSUMPTION] tags, user validates after
- (decision by user) storage: local-only, offline-capable, no sync/backup; data persists as long as the app stays installed — accepted risk, explicitly not solving data loss on uninstall/device change
- (decision by user) platforms: both iOS and Android targeted (React Native default); which gets more polish depends on available build time, not a scope cut
- (idea by user) primary user journey: user (Jas) opens the app specifically to copy a saved prompt and paste it into whichever AI tool they're using — that's the entire loop
- (assumption) categories feature flagged by user as lower priority ("nice to have for now") relative to Prompt Management/Search/Favorites/Copy — interpreted as: keep FR-9 (assign category) in MVP scope but mark it first-to-cut if the one-day timebox runs short; fixed 6-category list unchanged
- (gap) vision statement promises "organize, search, improve, and reuse" but the given MVP feature list has no "improve" capability (no versioning, no AI-assisted rewrite/critique) — surfaced as an Open Question, not silently dropped
- (event) drafted prd.md + addendum.md (Fast path, full draft with inline [ASSUMPTION] tags): 9 FRs across 5 features (Prompt Management, Search, Favorites, Copy, Categories), 1 user journey (UJ-1), 4 Open Questions, 7 indexed assumptions; Categories (FR-9) marked first-to-cut under time pressure per user's "nice to have" signal
- (decision by user) all 7 draft assumptions confirmed as-is: delete confirmation required, substring live search, empty search shows "no matches", plain-text-only copy, category optional, success metrics kept as self-assessed (no telemetry)
- (decision by user) empty-library state clarified: simple placeholder message; the persistent "+" button (not a separate CTA) is the add-prompt entry point everywhere, including the empty state
- (decision by user) explicit build order confirmed: Prompt Management, Search, Favorites, Copy all working before Categories (FR-9) is touched — reinforces FR-9 as last/first-to-cut
- (event) resolved all 7 [ASSUMPTION] tags into confirmed FR language; Assumptions Index (§9) now empty/closed; 4 Open Questions in §8 remain open, not addressed this round
- (decision by user) "improve" vision/scope gap resolved: explicitly deferred, requires LLM integration out of scope for this build
- (decision by user) category filtering added as FR-10, bundled at same "nice to have"/build-last priority as FR-9 category assignment — no longer out of scope
- (decision by user) empty states stay plain placeholder message only — no starter-prompt suggestions or extra polish
- (decision by user) copy stays plain-text-only, no tool-specific formatting for Claude/ChatGPT/etc. — reconfirmed
- (event) all 4 Open Questions in §8 resolved; §8 now closed with resolution log; PRD has zero outstanding assumptions or open questions
- (event) Finalize started: memlog audit clean (all 21 entries already reflected in prd.md/addendum.md), input reconciliation clean (brain dump fully covered, no gaps)
- (decision by user) reviewer gate: rubric walker only, no adversarial reviewer — user wants to avoid overcomplicating a deliberately simple PRD
- (event) rubric review complete (review-rubric.md): overall verdict Strong, all 7 dimensions strong, 0 critical/high, 1 medium, 4 low findings
- (change) fixed 4 of 5 rubric findings: clarified category-optionality vs. addendum's TS contract (addendum.md note added); fixed broken cross-ref §5→§2 to §1; fixed FR-4 typo "unrtruncated"→"untruncated"; untangled FR-9's optional-category rationale from its build-priority rationale
- (decision by user) 5th rubric finding (FR-8's "brief" visual confirmation) left as-is — a fixed duration would be an invented number nobody asked for, irrelevant to a simple app; all 5 rubric findings now resolved (4 fixed, 1 accepted as-is)
- (event) triage: one [NOTE FOR PM] remains (§6.2, data export/backup) — non-blocker, deferred; owner: Jas; revisit condition: if PromptForge continues past the assessment into real daily-use/longer-term project
- (change) structural polish: merged §8 Open Questions + §9 Assumptions Index (both fully resolved/closed) into one §8 Resolution Log; cut a build-priority note restated 3x down to one authoritative mention in §6.1; addendum.md needed no structural changes
- (change) prose polish: fixed stale §0 reference to removed §9/[ASSUMPTION] tags, a sentence fragment in §6.1, id/`id` formatting, and 2 minor cross-document terminology mismatches (AI-tool-agnostic, "no category set")
- (event) PRD finalized
