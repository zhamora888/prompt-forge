---
scope: PromptForge — full app (single feature-altitude spine, no epics split yet)
purpose: build-substrate spine; a separate human-facing reviewer doc comes later as its own artifact
altitude: feature
updated: 2026-07-08
---

- (event) sources: prd-prompt-forge-2026-07-08/prd.md + addendum.md (final), ux-prompt-forge-2026-07-08/DESIGN.md + EXPERIENCE.md (final)
- (decision by user) working mode: Coaching path — user's first time with React Native, wants the trade-offs walked through rather than assumed
- (decision by user) deliverable this run: spine only. A human-reviewer-facing writeup is wanted later, as a separate artifact (Finalize step 5 candidate)
- (constraint) [ADOPTED] stack pre-decided by PRD addendum: React Native (iOS + Android), NativeWind/Tailwind for styling — inherited, not re-decided
- (constraint) [ADOPTED] fully offline, local-only storage, no backend, no accounts — inherited from PRD §1/§5/§6
- (question) on-device storage engine explicitly left open by PRD addendum ("architecture decision, out of scope for PRD") — the primary open decision this run needs to resolve
- (version) web-verified 2026-07-08: Expo SDK 56 (May 2026 release), React Native 0.85, React 19.2, New Architecture mandatory (Legacy Architecture removed as of SDK 55+)
- (version) web-verified 2026-07-08: react-native-mmkv ~20-30x faster than AsyncStorage but is a native module requiring a custom EAS dev client build — breaks Expo Go compatibility
- (version) web-verified 2026-07-08: Expo SDK 56 ships Expo Router (file-based routing) as the default paved-path navigation; React Navigation still works underneath but is no longer expo-router's direct dependency as of SDK 56
- (decision by user) managed Expo workflow, not bare React Native CLI — user's personal machine isn't set up for native mobile dev (no working Xcode/Android Studio toolchain); Expo Go lets them test on-device without a native build step. Reason logged since it's unusually load-bearing for this call.
- (decision by user) local storage: AsyncStorage, single JSON blob holding the full prompt array, in-memory JS filter/search — "not designed to scale anyway, just a personal app for showcasing." Explicitly rejected expo-sqlite (no relational needs) and MMKV (breaks Expo Go, unnecessary perf for this data size).
- (decision by user) navigation: Expo Router (file-based), the current SDK 56 default — no real trade-off surfaced, user agreed with the paved-path recommendation
- (version) web-verified 2026-07-08: NativeWind v4 (stable, Tailwind v3) vs v5 (preview, CSS-first config) — both compatible with Expo SDK 56's mandatory New Architecture. Recommending v4 (stable) over v5 (preview) for a one-day build — not re-asked, low-stakes/non-forking call.
- (version) web-verified 2026-07-08: expo-clipboard and expo-crypto both version-aligned to SDK 56 (56.0.4). @react-native-async-storage/async-storage is a separate community package with its own versioning (search surfaced conflicting guidance — v2.2.0 "recommended for Expo" vs v3.1.1 "latest" vs a GitHub issue about Expo forcing v1.23.1); safest binding is "whatever `npx expo install` resolves," not a hardcoded number, since Expo's install command pins the SDK-56-compatible version automatically.
- (decision by user) code paradigm: single Repository module (e.g. `lib/promptRepository.ts`) as the sole gateway to AsyncStorage — "i dont want anyone else messing with data, except that module." No screen/component may import AsyncStorage directly.
- (decision by user) state management: React Context (`PromptsProvider` + `usePrompts()` hook), no external state library — "sounds simple enough... no need to use fancy libraries."
- (decision by user) confirmed proposed structural seed as-is: source tree (app/, components/, lib/promptRepository.ts, lib/PromptsProvider.tsx, types/prompt.ts), id via expo-crypto randomUUID(), ISO 8601 timestamps, storage errors logged not surfaced (no backend to retry against, not in PRD scope)
- (direction) all coaching-round decisions closed; distilling ARCHITECTURE-SPINE.md next
- (event) distilled ARCHITECTURE-SPINE.md: layered paradigm (Presentation→State→Repository→Storage), 3 ADs (single storage gateway, single state provider, navigation-mirrors-IA), consistency conventions, stack table (Expo 56/RN 0.85/React 19.2/Expo Router/NativeWind v4), structural seed (source tree + Prompt ERD), capability→architecture map (5 features), 5 Deferred items (EAS/store submission, dark mode, export/backup, testing strategy, category migration)
- (event) input reconciliation (reconcile-inputs.md) against all 4 sources: 5 unique gaps found — FR-2 updatedAt/createdAt rule, FR-7/FR-10 filter-combination state shape, fixed category list single-source-of-truth, "improve" deferral missing from Deferred, DetailActionRow/FormInput components missing from naming list; 1 new gap surfaced during reconciliation not caught during drafting (initial-hydration/loading-state convention, ties to EXPERIENCE.md's "no skeleton loaders" ban)
- (change) fixed all 6 gap citations (5 unique): AD-1 now states createdAt write-once/updatedAt-refreshes rule; AD-2 now states usePrompts() exposes 3 independent AND-combining filter fields not one exclusive mode; added lib/categories.ts as single source of truth for the fixed 6-category list; added DetailActionRow + FormInput to component naming list and structural seed; added expo-splash-screen-gated hydration convention so no screen ever needs a loading state; added "improve"/AI-assist to Deferred
- (event) mechanical floor checked manually (no lint_spine.py runner available): no template comments, no duplicate AD ids, all 3 ADs have Binds/Prevents/Rule, all Stack versions pinned or intentionally deferred-to-install-time
- (decision by user) reviewer gate: run it — rubric walker + both configured finalize_reviewers (version-verification, adversarial divergence-hunting)
- (event) 3 parallel reviews complete: rubric (strong/adequate, 0 high, 1 medium: design-token single-source gap, 2 low), version-verification (8/9 confirmed-current, 1 caveat: expo-splash-screen unverified + open upstream issue, NativeWind v4/v5 latest-tag transition risk), adversarial (8 divergence scenarios: AD-3 self-contradiction on route nesting, unspecified write concurrency/lost-update race, silent persistence failure, categoryFilter sentinel collision with Prompt.category's '', unowned favorite-toggle updatedAt behavior, plus 3 lower-severity)
- (change) fixed all findings directly (Finalize-gate convention: architect applies clear fixes, doesn't hand back a list): AD-1 now specifies single in-memory-source-of-truth writes (closes the race), dedicated toggleFavorite() not touching updatedAt, explicit accepted fire-and-forget persistence trade-off; AD-2 now types categoryFilter as string|null distinct from Prompt.category's ''; AD-3 rewritten to fix the real self-contradiction — routes are siblings under app/prompt/ (not nested), Edit reached via router.replace() not push() so stack depth stays 1; Structural Seed route tree and Capability Map corrected to match; added lib/theme.ts as DESIGN.md token single-source-of-truth; added Category union type derived from categories.ts; added trim-before-validate rule; clarified Toast state is local not global; pinned NativeWind to ^4.2.6 explicitly with a v5-transition caveat; added expo-splash-screen fallback note (plain conditional render) given an open upstream issue on preview builds
- (decision by user) reviewer-facing human doc: build now, not deferred to a later session
- (event) produced solution-design.html — a human-facing writeup (purpose: report) walking the reviewer through Vision, Paradigm, the 5 coached decisions with rejected alternatives shown, the 3 ADs, Stack, Structure, a condensed review-process/QA section surfacing the 4 high-severity adversarial catches, and Deferred. Visually grounded in the actual product's own DESIGN.md palette (indigo accent, neutral surfaces) for continuity between the app and the doc describing it.
- (change) polished solution-design.html: structural pass cut one duplicated passage (AD-3's rule-banner fully restated what §7 already covers — trimmed to a cross-reference); prose pass found no issues, terminology confirmed consistent with the spine
- (event) published solution-design.html as an Artifact for easy reviewer access
- (event) spine finalized
