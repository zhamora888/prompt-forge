import tokens from "./theme.tokens.js";

export const colors = tokens.colors;
export const typography = tokens.typography;
export const rounded = tokens.rounded;
export const spacing = tokens.spacing;
